<div class="container">
  <div class="center-block">
    <h1 class="text-center"><%= @content.title %></h1>
    <% if @comments.count > 0 %>
      <p>みんなのおすすめコメント</p>
      <%= render @comments %>
    <% else %>
      <p>おすすめコメントはまだありません</p>
    <% end %>
    <% if logged_in? %>
      <%= form_with model: @comments, url: content_comments_path(@content), local: true do |f| %>
        <%= f.text_field :body, placeholder: 'コメントを追加' %>
        <%= f.submit '送信' %>
      <% end %>
    <% else %>
      <p class="mt-3">※おすすめコメントを投稿するにはログインが必要です</p>
    <% end %>
  </div>
</div>